# ServiceAccount used by the workload.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: example-workload-unsigned-skipped
  namespace: spire

---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: example-workload-unsigned-skipped
  namespace: spire
  labels:
    app: example-workload-unsigned-skipped
spec:
  selector:
    matchLabels:
      app: example-workload-unsigned-skipped
  template:
    metadata:
      namespace: spire
      labels:
        app: example-workload-unsigned-skipped
        spire-workload: example-workload-unsigned-skipped
    spec:
      serviceAccountName: example-workload-unsigned-skipped
      hostPID: true
      hostNetwork: true
      dnsPolicy: ClusterFirstWithHostNet
      containers:
        - name: example-workload-unsigned-skipped
          image: docker-registry-local:5000/workload:alternate-unsigned
          imagePullPolicy: IfNotPresent
          command: ["/usr/bin/dumb-init", "/opt/spire/bin/spire-agent", "api", "watch"]
          args: ["-socketPath", "/tmp/spire-agent/public/api.sock"]
          volumeMounts:
            - name: spire-agent-socket
              mountPath: /tmp/spire-agent/public
              readOnly: true
      volumes:
        - name: spire-agent-socket
          hostPath:
            path: /run/spire/agent-sockets
            type: Directory
