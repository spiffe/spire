version: '3'
services:
  spire-server:
    image: spire-server:latest-local
    volumes:
      - ./conf/server:/opt/spire/conf/server
      - ./ctmkms:/opt/spire/ctmkms
    command: ["-config", "/opt/spire/conf/server/server.conf"]
