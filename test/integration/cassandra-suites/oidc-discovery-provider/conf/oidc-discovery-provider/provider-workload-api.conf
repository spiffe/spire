log_level = "DEBUG"
domains = ["localhost"]
listen_socket_path = "/opt/spire/conf/oidc-discovery-provider/provider-workload.sock"
workload_api {
    socket_path = "/opt/spire/conf/agent/workload_api.sock"
    trust_domain = "domain.test"
}
