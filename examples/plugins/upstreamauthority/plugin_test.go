package main

import "testing"

// Just here to make sure the plugin gets built as part of go test. The test
// itself exists to silence the "no tests to run" warning.
func TestBuild(t *testing.T) {}
