log_level = "debug"
trust_domain = "example.org"
server_socket_path = "/tmp/spire-server/private/api.sock"
cluster = "MyCluster"
# check_signature_enabled: If true, all entries generated by workloads
# supported by the Registrar will have entries added with
# the selector “"k8s:sigstore-validation:passed"”.
check_signature_enabled = false
